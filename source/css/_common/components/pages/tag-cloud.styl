.tag-cloud {
  text-align: center;

  a {
    display: inline-block;
    margin: 10px;
    padding: 5px;
    border-radius: 5px;

    &:hover {
      color: var(--link-hover-color) !important;
    }
  }
}
